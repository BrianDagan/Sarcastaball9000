{
  "name": "djdad",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Local development and regression tooling for the static DJDad app",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "test:publication": "node --test tests/publication.test.cjs",
    "check": "node --check docs/app.js && node --check scripts/validate-publication.cjs",
    "check:publication": "node scripts/validate-publication.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^26.1.0"
  }
}
